import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#05DD41',
      },
    },
    typography: {
        fontFamily:'Imprima'
    }
  });

export default theme